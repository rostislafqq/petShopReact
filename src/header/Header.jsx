import HeaderCart from './HeaderCart';
import HeaderLogo from './HeaderLogo';

const Header = () => {
  return (
    <header>
      <HeaderLogo />

      <HeaderCart />
    </header>
  );
};

export default Header;
